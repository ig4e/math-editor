// Side-effect barrel for graphers. Phase 4 registers jsxgraph2d;
// Phase 5 adds three3d. Each is itself lazy (the import here just
// registers metadata — the heavy lib is loaded on first plot).

import './jsxgraph2d';
